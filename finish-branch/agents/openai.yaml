interface:
  display_name: "Finish Branch"
  short_description: "Commit, push, and open pull requests with approval gates."
  default_prompt: "Inspect the branch state first, draft the required artifact, and wait for approval before any irreversible action."
policy:
  allow_implicit_invocation: true
