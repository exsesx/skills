interface:
  display_name: "Git Fatality"
  short_description: "Finalize branches through commit, push, and PR"
  brand_color: "#E11D48"
  default_prompt: "Use $git-fatality to inspect the current Git state, execute only the branch, commit, push, and PR actions I request, and verify the result."
policy:
  allow_implicit_invocation: true
